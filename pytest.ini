[pytest]
testpaths = tests/unit tests/integration
python_files = test_*.py
pythonpath = .
asyncio_mode = auto
addopts = -ra --import-mode=importlib --strict-markers
markers =
    integration_db: tests that require a PostgreSQL test database
