# 接口列表

> 更新日期：2026-06-04  第二轮接口补齐完成（新增 8 个端点 + 2 个路径别名 + 2 个新 Model）
> 状态口径：✅ 完成（含 PostgreSQL 数据库集成测试；或接口不依赖数据库）  🧪 代码完成/本地无DB测试，待数据库集成测试或外部联调  ⏳ 待开发

---

# 一、核心功能

> 用户从登录到完成报名缴费的最小闭环。

---

## 健康检查

✅ GET /health — 健康检查（含 DB/Redis 连通性检测，返回 ok/degraded）
✅ GET /ready — 就绪探针（K8s readiness probe）

---

## 登录认证模块

🧪 POST /api/auth/login — 微信 code 登录，返回 JWT token（代码完成 + HTTP 冒烟测试通过，待微信 code2session 联调）
🧪 POST /api/auth/refresh — 刷新 access_token（GETDEL 原子化防并发，代码完成 + 单元测试，待 Redis 联调）
🧪 POST /api/auth/logout — 退出登录，删除 refresh_token（代码完成）

---

## 用户模块

✅ DELETE /api/user/account — 注销账号（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）
🧪 POST /api/user/phone/decrypt — 解密微信手机号（代码完成 + HTTP 冒烟测试通过，待微信手机号解密联调）
✅ POST /api/user/identity — 提交实名认证信息（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，含身份证校验）
✅ GET /api/user/identity — 查询实名认证状态（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）

---

## 认证列表模块

✅ GET /api/cert/certifications — 多认证类型列表（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，支持 H3C/深信服/NISP/人社筛选）

---

## 课程模块

✅ GET /api/courses — 课程列表（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，支持类目筛选和分页）
✅ GET /api/courses/my — 我的课程列表（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，含学习权限状态和分页）
✅ GET /api/courses/{id} — 课程详情（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，含介绍/视频/班次/班主任联系方式）
🧪 POST /api/courses/enroll — 课程报名（代码完成 + HTTP 冒烟测试通过，未接入订单/支付闭环）

---

## 价格配置模块

✅ GET /api/prices — 价格配置列表（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，支持认证类型和用户类型筛选）

---

## 报名支付模块

✅ POST /api/orders — 创建订单（代码完成 + 单元测试 + DB集成测试，含库存原子锁定/过期时间/并发不超卖）
✅ GET /api/orders — 用户订单列表（代码完成 + 单元测试 + DB集成测试，支持分页和状态筛选含 closed）
✅ GET /api/orders/{id} — 订单详情（代码完成 + 单元测试 + DB集成测试，含 expires_at/closed_at/close_reason）
🧪 POST /api/payment/prepay — 微信支付统一下单（代码完成 + DB过期校验测试，严格依赖真实支付配置，待服务器/微信联调）
✅ POST /api/payment/callback — 支付回调通知（代码完成 + DB集成测试，含重复回调幂等/库存确认成交/交易号唯一约束）

---

## 客服对话模块

🧪 POST /api/chat — 发送消息，返回客服回复（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，需配置 CHAT_BACKEND=dify 后接入真实客服）
🧪 GET /api/chat/stream — SSE 流式消息响应（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，需配置 CHAT_BACKEND=dify 后接入真实客服流）

---

## 快速问题模块

✅ GET /api/quick-questions — 获取推荐问题列表（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，随机返回最多 6 条启用问题）

---

## 系统模块

✅ GET /api/system/poster — 获取登录海报 URL（代码完成，读取配置项）

---

# 二、额外功能

> 增强体验、运营支撑、后台管理。

---

## 人工客服转接模块

✅ POST /api/tickets — 工单创建（需登录，代码完成 + Service/Schema/API 已实现）
✅ GET /api/tickets — 用户工单列表（需登录，支持分页+倒序，代码完成）
✅ GET /api/tickets/{id} — 工单详情（需登录，仅可查自己的工单，代码完成）

---

## 专区内容模块

✅ GET /api/zones — 首页聚合（Banner 按 sort + 各 zone_type 专区条目，代码完成）
✅ GET /api/zones/cert — 认证专区（zone_type=cert + Certification 表数据，代码完成）
✅ GET /api/zones/study — 学习专区（zone_type=study + Course 表数据，代码完成）
✅ GET /api/zones/competition — 竞赛专区（zone_type=competition + CompetitionReg 数据，代码完成）
✅ GET /api/zones/activity — 活动专区（zone_type=activity + Activity 表数据，代码完成）
✅ GET /api/zones/employment — 就业专区（zone_type=employment + Job 表数据，代码完成）

---

## 题库模块

> 全部接口已完成单元测试、PostgreSQL 数据库集成测试和 HTTP 冒烟测试。

✅ GET /api/quiz/categories — 题库分类树（公开，递归构建多级子分类，循环引用保护）
✅ GET /api/quiz/questions — 题目列表（公开，支持 category_id / question_type 筛选 + 分页，不泄露 correct_answer）
✅ POST /api/quiz/submit — 答题提交 + 即时判分（需登录，支持 single_choice / multiple_choice / judge 三种题型，中文判断题答案「对/错」归一化为 TRUE/FALSE，答错自动入错题本）
✅ GET /api/quiz/wrong-book — 错题本列表（需登录，用户隔离 + 分页，返回题目详情不含答案）
✅ POST /api/quiz/wrong-book — 错题本添加（需登录，无答题记录时自动创建，幂等）
✅ DELETE /api/quiz/wrong-book/{id} — 错题本移除（需登录，将 is_wrong 置为 false，非物理删除）
✅ GET /api/quiz/collections — 收藏列表（需登录，用户隔离 + 分页，返回题目详情不含答案）
✅ POST /api/quiz/collections — 收藏添加（需登录，无答题记录时自动创建，幂等）
✅ DELETE /api/quiz/collections/{id} — 收藏移除（需登录，将 is_collected 置为 false，非物理删除）
✅ GET /api/quiz/checkin — 打卡状态查询（需登录，返回当天打卡状态、连续天数、最后一次打卡日期）
✅ POST /api/quiz/checkin — 打卡签到（需登录，重复签到幂等返回已有记录，连续天数基于昨天签到记录自动计算）

---
## 积分模块

✅ GET /api/points — 积分余额查询（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，新用户返回 0 不报错）
✅ GET /api/points/history — 积分获取/消耗记录（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，用户隔离 + 分页）
✅ POST /api/points/claim — 领取积分（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，支持领取幂等/并发不重复发放）
✅ POST /api/points/redeem — 积分兑换（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过，余额不足拒绝/并发不透支）

---

## 优惠券模块

✅ GET /api/coupons — 用户优惠券列表（需登录，支持 status 筛选+分页，代码完成）
✅ POST /api/coupons/assign — 优惠券领取（需登录，校验有效期+防重复领取，代码完成）
✅ POST /api/coupons/verify — 优惠券核销（需登录，校验已领取+未使用+未过期，代码完成）
🧪 POST /api/coupons/validate — 核销别名（等价于 /verify，兼容旧前端路径，代码完成）

---

## 电子协议模块

✅ GET /api/agreements — 用户协议列表（需登录，支持类型筛选+分页，代码完成）
✅ POST /api/agreements — 协议创建（需登录，关联培训协议模板，代码完成）
✅ PUT /api/agreements/{id}/sign — Canvas 签名图片上传（需登录，校验所有权，代码完成）

---

## 收藏模块

🧪 GET /api/collections — 用户收藏列表（需登录，支持 target_type 筛选+分页，代码完成，待 DB 集成测试）
🧪 POST /api/collections — 添加收藏（需登录，防重复，代码完成，待 DB 集成测试）
🧪 DELETE /api/collections/{id} — 取消收藏（需登录，校验所有权，代码完成，待 DB 集成测试）

---

## 竞赛统计模块

✅ GET /api/competition/stats — 按学校统计报名人数（GROUP BY school ORDER BY count DESC，代码完成）
✅ GET /api/competition/export — 导出当前用户报名记录为 CSV（需登录，代码完成）
🧪 GET /api/competition/tracks — 赛道列表（不重复 track 值，代码完成，待 DB 集成测试）
🧪 POST /api/competition/signup — 竞赛报名（需登录，传 competition_name/school/track，代码完成，待 DB 集成测试）

---

## 活动模块

🧪 GET /api/activities — 活动列表（仅展示进行中且未结束，支持分页，代码完成，待 DB 集成测试）
🧪 POST /api/activities/register — 活动报名（需登录，校验有效性+名额+防重复，代码完成，待 DB 集成测试）
🧪 GET /api/activities/export — 导出当前用户活动报名 CSV（需登录，代码完成，待 DB 集成测试）
🧪 POST /api/activities/{id}/remind — 设置活动提醒（需登录，ActivityReminder 表，去重，代码完成，待 DB 集成测试）
🧪 POST /api/activities/{id}/enroll — 活动报名别名（等价于 /register，兼容旧前端路径，代码完成）

---

## 分享模块

✅ POST /api/share — 分享链接生成（需登录，secrets.token_hex(4) 生成唯一码，代码完成）
✅ GET /api/share/{code} — 分享记录追踪（visit_count+1，返回目标信息，代码完成）

---

## OSS 存储模块

✅ POST /api/upload — 文件上传（需登录，本地存储 uploads/，UUID 文件名，代码完成）
✅ GET /api/media/{id} — 文件访问/下载（无需登录，FileResponse 返回，代码完成）

---

## 就业招聘模块

🧪 GET /api/jobs — 招聘岗位列表（仅 is_active=true，支持分页+倒序，代码完成，待 DB 集成测试）
🧪 POST /api/jobs/{id}/apply — 岗位投递（需登录，JobApplication 表，防重复，代码完成，待 DB 集成测试）

---

## 认证详情 / 标签

🧪 GET /api/cert/certifications/{id} — 认证项目详情（含创建/更新时间戳，代码完成，待 DB 集成测试）
🧪 GET /api/cert/certifications/tags — 厂商标签列表（H3C/深信服/NISP/人社，静态枚举，代码完成）

---

## 课程类目

🧪 GET /api/courses/categories — 获取所有不重复的课程类目（代码完成，待 DB 集成测试）

---

## 用户个人信息

🧪 GET /api/user/profile — 获取用户个人信息（含脱敏手机号、实名信息，代码完成，待 DB 集成测试）
🧪 PUT /api/user/profile — 编辑个人信息（重新绑定手机号，含 edit_count 限制校验，代码完成，待 DB 集成测试）

---


# 三、后台

> 管理后台 API，与 C 端小程序 API 在同一 FastAPI 应用中，共享 DB/Redis，认证独立。
> admin 角色：super_admin | content_editor | customer_service | finance | auditor

---

## 管理后台认证

✅ POST /admin/auth/login — 账号密码登录，返回 JWT（type=admin，含 role + permissions），5/min 限流
✅ GET /admin/auth/me — 获取当前管理员信息 + 角色对应权限列表（代码完成 + HTTP 集成测试通过）
✅ POST /admin/auth/logout — 退出登录（代码完成 + HTTP 集成测试通过）

---

## 用户管理

✅ GET /admin/users — 用户列表 + 分页 + openid/phone/created_at_start/created_at_end 筛选
✅ GET /admin/users/{id} — 用户详情
✅ PUT /admin/users/{id} — 封禁/解封（is_active）
✅ PATCH /admin/users/{id}/status — 切换用户启用/禁用状态（代码完成 + 单元测试 + DB 集成测试通过）
✅ POST /admin/users/batch-delete — 批量软删除（代码完成 + DB 集成测试通过）
✅ GET /admin/users/export — 用户数据 CSV 导出（代码完成 + DB 集成测试通过）
✅ GET /admin/users/{id}/orders — 用户订单记录（代码完成 + DB 集成测试通过）
✅ GET /admin/users/{id}/conversations — 用户对话记录（代码完成 + DB 集成测试通过）

---

## 订单管理

✅ GET /admin/orders — 订单列表 + cert_type/phone 筛选 + 状态筛选 + 时间筛选 + 分页
✅ GET /admin/orders/{id} — 订单详情（含 transaction_id、paid_at）
✅ POST /admin/orders/{id}/refund — 退款（status→refunded），预留微信退款对接点
✅ GET /admin/orders/export — 订单 CSV 导出（代码完成 + DB 集成测试通过）
✅ GET /admin/orders/reconciliation — 对账数据（按日期汇总：order_total/refund_total/net_income/order_count，代码完成 + DB 集成测试通过）

---

## 课程管理

✅ POST /admin/courses — 创建课程
✅ PUT /admin/courses/{id} — 编辑课程（含班次 JSON）
✅ DELETE /admin/courses/{id} — 下架（is_active=false，非物理删除）
✅ GET /admin/courses — 全量列表（含已下架），支持 keyword 关键词搜索 + category 分类筛选

---

## 认证管理

✅ POST /admin/certifications — 创建认证类型
✅ PUT /admin/certifications/{id} — 编辑认证类型

---

## 价格配置

✅ POST /admin/prices — 新增价格行
✅ PUT /admin/prices/{id} — 修改价格
✅ DELETE /admin/prices/{id} — 停用价格

---

## 题库管理

✅ GET /admin/quiz/categories — 分类列表（平铺，quiz:list 权限，代码完成 + DB 集成测试通过）
✅ GET /admin/quiz/questions — 题目列表（含 correct_answer，支持分类/题型筛选+分页，quiz:list 权限，代码完成 + DB 集成测试通过）
✅ POST /admin/quiz/categories — 创建分类
✅ PUT /admin/quiz/categories/{id} — 编辑分类
✅ DELETE /admin/quiz/categories/{id} — 删除分类
✅ POST /admin/quiz/questions — 创建题目（question_type 枚举校验：single_choice/multiple_choice/judge）
✅ PUT /admin/quiz/questions/{id} — 编辑题目
✅ DELETE /admin/quiz/questions/{id} — 删除题目
✅ POST /admin/quiz/questions/batch-delete — 题目批量删除（代码完成 + DB 集成测试通过）
✅ POST /admin/quiz/import — CSV 批量导入（支持 UTF-8/GBK，含中英文表头，5MB+CSV类型校验）
✅ POST /admin/quiz/import/json — JSON 批量导入（代码完成 + DB 集成测试通过）

---

## 专区内容

✅ POST /admin/zones — 创建专区内容
✅ PUT /admin/zones/{id} — 编辑 + 排序
✅ DELETE /admin/zones/{id} — 下架
✅ GET /admin/zones — 全量列表，支持 keyword 关键词搜索
✅ PATCH /admin/zones/{id}/status — 内容上下架切换（代码完成 + DB 集成测试通过）
✅ POST /admin/zones/batch-delete — 批量下架（代码完成 + DB 集成测试通过）
✅ PUT /admin/zones/sort — 内容排序（批量更新 sort_order，代码完成 + DB 集成测试通过）

---

## Banner管理

✅ GET /admin/banners — Banner 全量列表（不分页，代码完成 + DB 集成测试通过）
✅ POST /admin/banners — 创建 Banner（代码完成 + DB 集成测试通过）
✅ PUT /admin/banners/{id} — 编辑 Banner（代码完成 + DB 集成测试通过）
✅ DELETE /admin/banners/{id} — 删除 Banner（代码完成 + DB 集成测试通过）
✅ POST /admin/banners/batch-delete — 批量删除 Banner（代码完成 + DB 集成测试通过）

---

## 优惠券管理

✅ GET /admin/coupons — 优惠券库列表
✅ POST /admin/coupons — 单个创建
✅ POST /admin/coupons/batch — 批量创建（随机码，最多 1000 张）
✅ DELETE /admin/coupons/{id} — 作废

---

## 协议管理

✅ GET /admin/agreements — 协议列表
✅ POST /admin/agreements — 创建模板（富文本 content）
✅ PUT /admin/agreements/{id}/review — 审核 + 盖章

---

## 工单管理

✅ GET /admin/tickets — 工单列表 + 状态筛选（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）
✅ PUT /admin/tickets/{id} — 处理工单（分配老师/关闭，代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）

---

## 数据看板

✅ GET /admin/statistics/dashboard — 总用户数/订单数/营收/转化率（近 30 天，代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）

---

## 系统设置

✅ GET /admin/settings/admins — 管理员列表（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）
✅ POST /admin/settings/admins — 创建管理员（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）
✅ PUT /admin/settings/admins/{id} — 修改角色/启用禁用（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）

---

## 竞赛导出

✅ GET /admin/competition/export — CSV 导出（代码完成 + 单元测试 + DB集成测试 + HTTP 冒烟测试通过）
