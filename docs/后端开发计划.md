# 后端开发计划

> 编制日期：2026-05-14
> 项目启动日：2026-05-14（周四）
> 技术栈：Python FastAPI + PostgreSQL + Redis
> 模块总量：22 个后端 API 模块 + DevOps
> 人员：1 名后端工程师（可获前端工程师支援）

---

## 一、里程碑（后端视角）

| 里程碑 | 日期 | 后端验收标准 |
|--------|------|------------|
| M1 基础环境就绪 | **5月18日** | 服务器/OSS/DB/Redis/Nginx 就绪 |
| M2 核心 API 可用 | **5月22日** | 用户/认证/客服/课程 4 大模块 API 可调用（缴费系统延后） |
| M3 全部 API 就绪 | **6月12日** | 22 个后端模块全部上线（含缴费系统） |
| M4 提测 | **6月18日** | 全量接口测试通过、性能达标 |
| M5 上线 | **6月24日** | 生产环境部署、正式支付切换、数据初始化 |

---

## 二、Phase 0：基础设施（5月14日 — 5月18日）

### Day 1 — 5月14日（周四）项目启动

| 任务 | 工时 | 产出 |
|------|------|------|
| FastAPI 项目脚手架搭建（目录结构/中间件/配置管理/日志） | 3h | 仓库初始化 |
| PostgreSQL 数据库设计（13 张表：用户/订单/课程/题库/积分/优惠券/协议/竞赛统计/认证） | 4h | DDL Migration 就绪 |

**数据库表清单（13 张）**：

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `user` | 用户表 | openid, phone, user_type, age_group, certification_interest |
| `order` | 订单表 | user_id, cert_type, candidate_name/phone/idcard, price, status, out_trade_no |
| `conversation` | 会话表 | user_id, session_id, messages(JSON) |
| `price_config` | 价格配置表 | cert_type, user_type, price, is_active |
| `certification` | 认证信息表 | name, code, vendor, price_enterprise, price_student, requires_xuexin, pay_first |
| `course` | 课程表 | title, category, description, cover_url, video_url, price, batches(JSON), teacher_name, teacher_contact |
| `course_enrollment` | 课程报名表 | user_id, course_id, batch_selected, status, learning_access |
| `quiz_category` | 题库分类表 | name, parent_id, description |
| `quiz_question` | 题目表 | category_id, question_type, question_text, options(JSON), correct_answer, explanation |
| `quiz_record` | 答题记录表 | user_id, question_id, user_answer, is_correct, is_collected, is_wrong |
| `quiz_checkin` | 打卡表 | user_id, checkin_date, questions_completed, consecutive_days |
| `user_points` / `points_history` | 积分表 | user_id, balance / action_type, amount |
| `coupon` / `user_coupon` | 优惠券表 | code, type, value / user_id, coupon_id, status |
| `agreement` | 电子协议表 | user_id, type, content, signature_image, status |
| `competition_reg` | 竞赛报名表 | user_id, competition_name, school, track |
| `ticket` | 客服工单表 | user_id, teacher_id, content, status |

### Day 2 — 5月15日（周五）基础设施

| 任务 | 工时 | 产出 |
|------|------|------|
| 云服务器初始化（应用服务器 + DB + Redis + Nginx + 防火墙） | 4h | 环境就绪 |
| OSS 存储桶创建 + CDN 加速配置 + CORS 策略 | 2h | 存储服务就绪 |
| 域名 + SSL 证书绑定（Nginx HTTPS 配置） | 1h | HTTPS 可访问 |
| 微信小程序服务器域名白名单配置 | 1h | request 合法域名配置 |
| 微信支付沙箱环境配置（商户号/API密钥/证书） | 2h | 沙箱支付可拉起 |

### Day 3 — 5月18日（周一）基础收尾

| 任务 | 工时 | 产出 |
|------|------|------|
| 本地开发环境收尾（DB Migration 验证 + Redis 调试 + 联调确认） | 4h | 本地开发环境稳定 |
| 前后端接口协议确认（接口路径/字段/错误码对齐前端文档） | 3h | 接口文档定稿 |
| **M1 验收** | 1h | 环境+登录链路可演示 |

---

## 三、Phase 1：核心 API 开发（5月19日 — 5月29日）

### Day 4-5 — 5月19-20日 用户 + 客服模块

#### 用户服务模块 `B-01`（P0）

| 任务 | 工时 |
|------|------|
| `POST /api/auth/login` — 微信 code 换取 openid + 签发 JWT | 2h |
| `GET /api/user/profile` — 获取用户资料 | 1h |
| `PUT /api/user/profile` — 更新用户资料（修改次数限制） | 2h |
| `DELETE /api/user/account` — 账号注销 | 1h |
| 手机号解密存储（getPhoneNumber 敏感数据解密） | 2h |
| JWT Token 刷新 + 过期处理中间件 | 2h |

#### 客服对话模块 `B-02`（P1）

> 当前实现消息收发骨架 + 快速问题配置，AI 智能客服能力后续接入。

| 任务 | 工时 |
|------|------|
| `POST /api/chat` — 消息接收 + 会话存储（Redis），预留 AI 服务转发扩展点 | 2h |
| `GET /api/chat/stream` — SSE 流式响应骨架（预留 AI 流式输出接入） | 2h |
| `GET /api/quick-questions` — 推荐问题列表（后台可配置） | 1h |
| 对话上下文管理（Redis 会话存储） | 2h |

### Day 6 — 5月19日 认证列表 `B-07`（P0）✅

> 报名缴费、价格配置、微信支付延后开发，见下方「缴费系统（延后）」。

| 任务 | 工时 |
|------|------|
| `GET /api/certifications` — 多认证类型列表（H3C+深信服+NISP+人社） | 2h |
| 考试代码与中文全称自动匹配逻辑 | 2h |
| `GET /api/cert/export` — 报名信息导出 Excel | 2h |
| 学信网验证引导数据接口 | 1h |

### Day 7-8 — 5月20-21日 课程 + 安全

#### 课程/视频服务 `B-13`（P0 新增）

| 任务 | 工时 |
|------|------|
| `GET /api/courses` — 课程列表（华三/深信服类目筛选） | 2h |
| `GET /api/courses/:id` — 课程详情（介绍/时间/评价/班次/班主任联系方式） | 2h |
| `POST /api/courses/enroll` — 课程报名缴费 | 2h |
| `GET /api/courses/my` — 我的课程列表 + 学习权限状态 | 2h |
| 视频 OSS 直传签名 URL 生成 | 1h |

#### 安全模块

| 任务 | 工时 |
|------|------|
| 通用 XSS/SQL 注入防护中间件 | 2h |
| 请求频率限制（Rate Limiting，基于 Redis） | 2h |
| 敏感操作日志记录（支付/退款/管理员操作） | 1h |

### Day 9 — 5月22日 联调 + M2

| 任务 | 工时 |
|------|------|
| 前后端联调（登录 / 认证列表 / 客服 / 课程） | 4h |
| 核心 API 集成测试 | 2h |
| Bug 修复 | 2h |
| **M2 验收** | 1h |

---

## 四、缴费系统（延后开发）

> 报名缴费 + 价格配置 + 微信支付是核心关键功能，链条紧密。
> 待其他模块开发稳定后集中攻坚，计划在 Phase 2 全部 API 完成后切入。

### 报名缴费模块 `B-04`（P0）

| 任务 | 工时 |
|------|------|
| `POST /api/orders` — 订单创建（含表单校验） | 3h |
| 订单状态机实现（待支付→已支付→已完成/已退款） | 2h |
| `GET /api/orders` — 用户订单列表（分页+Tab 筛选） | 1h |
| `GET /api/orders/{id}` — 订单详情 | 1h |
| NISP 特殊流程：先支付后填表（订单类型区分） | 2h |
| 深信服报名：优惠券自动下发 + 动态验证码对接 | 2h |

### 价格配置模块 `B-05`（P0）

| 任务 | 工时 |
|------|------|
| PRICE_CONFIG 表 CRUD API | 2h |
| 多认证类型 + 多用户类型价格映射查询 | 1h |
| 后台价格配置管理接口 | 2h |

### 微信生态对接模块 `B-09`（P0）

| 任务 | 工时 |
|------|------|
| `POST /api/payment/prepay` — 微信支付统一下单 | 3h |
| 支付回调处理（幂等 + 签名验证 + 订单状态更新） | 3h |
| 订阅消息推送（报名成功/课程开通/考试提醒） | 2h |
| access_token 自动刷新管理（Redis） | 1h |

---

## 五、Phase 2：全部 API 开发（5月25日 — 6月12日）

### Day 13-14 — 6月1-2日 专区 + 竞赛统计

#### 专区内容服务 `B-08`（P1）

| 任务 | 工时 |
|------|------|
| `GET /api/zones` — 专区聚合页数据（Grid 2x2 + 热门推荐） | 2h |
| `GET /api/zones/cert` — 认证专区内容列表 | 2h |
| `GET /api/zones/study` — 学习专区内容列表（课程数据） | 1h |
| `GET /api/zones/competition` — 竞赛专区内容列表 | 1h |
| `GET /api/zones/activity` — 活动专区内容列表 | 1h |
| `GET /api/zones/employment` — 就业专区内容列表 | 1h |
| `CRUD /api/admin/zones` — 后台专区内容管理（增删改排序上下线+Banner配置+竞赛地址年度更新） | 4h |

#### 竞赛统计服务 `B-19`（P2 新增）

| 任务 | 工时 |
|------|------|
| `GET /api/competition/stats` — 报名人数统计（按学校分组） | 2h |
| `GET /api/competition/export` — Excel 导出 | 1h |

### Day 15-16 — 6月3-5日 题库中心（P1 新增）

| 任务 | 工时 |
|------|------|
| `GET /api/quiz/categories` — 题库分类树（章节层级） | 1h |
| `GET /api/quiz/questions` — 题目列表（按分类/分页/仅选择题） | 2h |
| `POST /api/quiz/submit` — 答题提交 + 即时判分（比对 correct_answer） | 2h |
| `GET/POST/DELETE /api/quiz/wrong-book` — 错题本 CRUD | 2h |
| `GET/POST/DELETE /api/quiz/collections` — 收藏 CRUD | 2h |
| `GET/POST /api/quiz/checkin` — 打卡记录与连续天数统计 | 2h |
| 题库批量导入工具（Excel → DB，含格式校验） | 4h |
| 模考/测评/刷题挑战模式配置 | 2h |

### Day 17-18 — 6月5-8日 用户扩展

#### 用户服务扩展 `B-01 扩展`

| 任务 | 工时 |
|------|------|
| 登录海报弹窗配置接口（1280×1080 竖版海报 URL 管理） | 1h |
| 考试意向字段（用户注册时采集） | 1h |
| 个人资料修改次数限制逻辑 | 1h |
| 报考查询接口（链接列表） | 1h |

#### 分享服务 `B-20`（P2 新增）

| 任务 | 工时 |
|------|------|
| `POST /api/share` — 分享链接生成（小程序/课程） | 2h |
| `GET /api/share/:code` — 分享记录追踪 | 1h |
| 拼团逻辑（后期实现，预留接口） | — |

### Day 19 — 6月9日 积分 + 优惠券（P1 新增）

#### 积分服务 `B-15`

| 任务 | 工时 |
|------|------|
| `GET /api/points` — 积分余额查询 | 1h |
| `POST /api/points/redeem` — 积分兑换（考试费减免：月减50上限100） | 2h |
| `POST /api/points/redeem` — 积分兑换课程 | 1h |
| `GET /api/points/history` — 积分获取/消耗记录 | 1h |
| 积分规则引擎（消费返积分、打卡赚积分） | 2h |

#### 优惠券服务 `B-16`

| 任务 | 工时 |
|------|------|
| `GET /api/coupons` — 用户优惠券列表 | 1h |
| `POST /api/coupons/assign` — 预设库自动下发（深信服场景） | 2h |
| `POST /api/coupons/verify` — 优惠券核销验证 | 1h |
| 优惠券复制（返回明文券码） | 0.5h |
| 后台优惠券库管理（批量创建/状态管理） | 2h |

### Day 20 — 6月10日 客服 + 协议（P1 新增）

#### 人工客服转接模块 `B-06`（P1）

| 任务 | 工时 |
|------|------|
| 转人工触发逻辑（主动/自动/关键词） | 2h |
| `POST /api/tickets` — 工单创建 + 老师分配 | 2h |
| 会话状态管理（waiting_manual→in_manual_chat→resolved） | 2h |
| 老师信息配置接口（姓名/微信/QQ/电话/工作时间） | 1h |

#### 电子协议服务 `B-17`（P1 新增）

| 任务 | 工时 |
|------|------|
| `GET /api/agreements` — 用户协议列表 | 1h |
| `POST /api/agreements` — 协议创建（关联培训协议模板） | 1h |
| `PUT /api/agreements/:id/sign` — 上传电子签名图片 | 1h |
| 协议状态流转引擎（发送→提交→审核→盖章→完成） | 2h |
| 后台协议管理（模板 CRUD + 审核 + 盖章确认） | 3h |

### Day 21 — 6月11日 OSS + NISP 专项（P1 新增）

#### OSS 存储服务 `B-18`

| 任务 | 工时 |
|------|------|
| `POST /api/upload` — 文件上传（图片/寸照/视频） | 2h |
| `GET /api/media/:id` — 获取文件访问 URL | 1h |
| NISP 身份证图片→PDF 转换（以考生姓名命名） | 2h |
| 姓名拼音自动生成（pypinyin 库） | 1h |
| 学籍报告查询通道配置 | 1h |

### Day 22 — 6月12日 管理后台 + M3

#### 管理后台 API `B-23`（P2）

| 任务 | 工时 |
|------|------|
| 用户管理 CRUD（列表/筛选/标签/分层） | 2h |
| 订单管理 CRUD（退款/标记异常/对账导出） | 2h |
| 数据统计分析（报名转化率/营收/竞赛统计） | 2h |
| 系统配置（管理员账户/权限/通知模板） | 1h |

### Day 22 全量联调 + M3

| 任务 | 工时 |
|------|------|
| 全量 API 前后端联调（课程权限/积分兑换/协议流转/竞赛统计等交叉验证） | 4h |
| **M3 验收** | 1h |

---

## 六、AI 智能客服（扩展预留）

> AI 智能客服（Dify + Ollama）为独立开发轨道，详见 **[AI 工作流开发计划](../../Customer/docs/AI工作流开发计划.md)**。
> 后端已预留对接点，后续接入时无需改动业务模块。

**后端已预留的对接点**：

| 对接点 | 后端模块 | 说明 |
|--------|---------|------|
| 对话入口转发 | B-02 `POST /api/chat` | Service 层预留 `ChatBackend` 抽象接口（含 `user_id`），当前实现为人工客服消息，后续注入 AI 实现即可 |
| 流式响应通道 | B-02 `GET /api/chat/stream` | SSE 管道已就绪，后续接入 AI 流式输出 |
| 转人工工单创建 | B-06 `POST /api/tickets` | AI 识别到需转人工时直接调用 |
| 报名卡片数据 | B-04 + B-07 | 认证列表/价格数据供 AI 意图触发后自动拉取 |

---

## 七、Phase 3：测试与优化（6月15日 — 6月18日）

### 6月15-16日 后端专项测试

| 测试项 | 内容 | 工时 |
|--------|------|------|
| API 单元测试 | 22 个模块核心接口覆盖率 ≥ 80% | 4h |
| 支付全链路测试 | 统一下单→回调处理→幂等验证→退款 | 2h |
| 题库判分测试 | 选择题正误判断 / 错题自动收录 / 打卡连续天数 | 2h |
| 积分/优惠券测试 | 兑换上限校验 / 优惠券下发幂等 / 核销状态流转 | 2h |
| 协议流转测试 | 发送→签名→审核→盖章全状态机 | 1h |
| NISP 专项测试 | 先付后填 / 身份证→PDF / 拼音生成 / 学籍报告 | 2h |
| 并发压力测试 | 支付并发 / 题库并发提交 / 报名并发 | 3h |

### 6月17日 性能优化

| 优化项 | 目标 | 工时 |
|--------|------|------|
| 数据库索引优化（高频查询字段） | 慢查询 < 100ms | 2h |
| Redis 缓存策略（认证列表/课程列表/题库分类） | 缓存命中率 > 80% | 2h |
| OSS CDN 缓存策略 | 视频首帧 < 3s | 1h |

### 6月18日 Bug 修复 + M4 提测

| 任务 | 工时 |
|------|------|
| Bug 集中修复 | 5h |
| 全量回归测试 | 2h |
| **M4 提测** | — |

---

## 八、Phase 4：部署上线（6月19日 — 6月24日）

### 6月19-22日 生产环境部署

| 任务 | 工时 |
|------|------|
| 生产服务器部署（FastAPI + Supervisor/systemd + Nginx 反代） | 3h |
| PostgreSQL 生产实例初始化 + Migration 执行 + 主从配置 | 2h |
| Redis 生产实例配置（持久化 + Key 过期策略） | 1h |
| OSS 正式 Bucket 配置 + CORS + CDN | 1h |
| 微信支付沙箱→正式环境切换（商户证书/回调地址） | 2h |
| 运维监控部署（进程监控/API 响应时间/错误率告警） | 2h |

### 6月22日 提交审核

| 任务 | 工时 |
|------|------|
| 小程序代码上传 + 体验版测试 + 提交审核 | 配合前端 |
| 管理后台部署 + 管理员账号初始化 | 2h |

### 6月22-24日 数据初始化

| 任务 | 工时 |
|------|------|
| 价格配置数据初始化（多认证类型×多用户类型） | 1h |
| 认证专区内容导入（H3C+深信服+NISP+人社信息） | 2h |
| 题库数据导入（甲方提供的 Excel 题库） | 2h |
| 优惠券预设库初始化（深信服券码批量导入） | 1h |
| 管理员账号创建 + 权限分配 | 1h |
| 课程 + 竞赛 + 活动 + 就业初始内容填充 | 2h |

### 6月24日 上线 + M5

| 任务 | 工时 |
|------|------|
| 线上冒烟测试（全核心 API 验证） | 1h |
| 文档交付（部署文档/接口文档/运维手册） | 3h |
| **M5 上线交付** | — |

---

## 九、后端 API 全量清单（33 个接口）

```
用户模块 (5)                    报名支付 (7)                    课程模块 (4)
├── POST   /api/auth/login      ├── GET    /api/certifications   ├── GET    /api/courses
├── GET    /api/user/profile    ├── GET    /api/cert/export      ├── GET    /api/courses/:id
├── PUT    /api/user/profile    ├── POST   /api/orders           ├── POST   /api/courses/enroll
├── DELETE /api/user/account    ├── GET    /api/orders           ├── GET    /api/courses/my
└──                              ├── GET    /api/orders/:id
                                 ├── POST   /api/payment/prepay   客服对话 (3)
                                 └── GET    /api/payment/callback ├── POST   /api/chat
                                                                 ├── GET    /api/chat/stream
专区内容 (6)                    题库模块 (6)                     └── GET    /api/quick-questions
├── GET    /api/zones           ├── GET    /api/quiz/categories
├── GET    /api/zones/cert      ├── GET    /api/quiz/questions    积分/优惠券 (6)
├── GET    /api/zones/study     ├── POST   /api/quiz/submit       ├── GET    /api/points
├── GET    /api/zones/competition├── CRUD   /api/quiz/wrong-book  ├── POST   /api/points/redeem
├── GET    /api/zones/activity  ├── CRUD   /api/quiz/collections ├── GET    /api/points/history
└── GET    /api/zones/employment├── GET    /api/quiz/checkin      ├── GET    /api/coupons
                                                                 ├── POST   /api/coupons/assign
客服/协议/分享 (6)              竞赛/OSS/NISP (5)                └── POST   /api/coupons/verify
├── POST   /api/tickets         ├── GET    /api/competition/stats
├── POST   /api/agreements      ├── GET    /api/competition/export  管理后台 (CRUD)
├── GET    /api/agreements      ├── POST   /api/upload              ├── /api/admin/users
├── PUT    /api/agreements/:id  ├── GET    /api/media/:id           ├── /api/admin/orders
├── POST   /api/share           └── POST   /api/cert/export         ├── /api/admin/zones
└── GET    /api/share/:code                                        ├── /api/admin/courses
                                                                    ├── /api/admin/quiz
                                                                    ├── /api/admin/coupons
                                                                    ├── /api/admin/agreements
                                                                    └── /api/admin/settings
```

---

## 十、模块间依赖关系（开发顺序约束）

```
                                    ┌──────────────┐
                                    │ 用户服务 B-01 │
                                    └──────┬───────┘
                                           │
                    ┌──────────────────────┼──────────────────────┐
                    │                      │                      │
                    ▼                      ▼                      ▼
         ┌─────────────┐      ┌──────────────┐      ┌──────────────┐
         │ 客服对话 B-02│      │ 认证列表 B-07 │      │ 课程 B-13    │
         └──────────────┘      └──────────────┘      └──────────────┘
            M2 范围                M2 范围               M2 范围

缴费系统（延后开发）：
                              ┌──────────────┐
                              │ 认证列表 B-07 │ (已完成)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 报名缴费 B-04 │ (延后)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 价格配置 B-05 │ (延后)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 微信支付 B-09 │ (延后)
                              └──────────────┘

第二阶段（可并行）：
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│专区 B-08  │  │题库 B-14 │  │积分 B-15 │  │优惠券B-16│  │客服 B-06 │
└──────────┘  └──────────┘  └────┬─────┘  └────┬─────┘  └──────────┘
                                 │              │
                                 └──────┬───────┘
                                        │
                                        ▼
                                 ┌──────────────┐
                                 │ 协议 B-17    │
                                 └──────────────┘

第三阶段（独立）：
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│NISP B-18│  │竞赛B-19  │  │分享B-20  │  │管理B-23  │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
```

---

## 十一、风险管理

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 微信支付回调签名验证失败 | 中 | 支付流程阻断 | 提前用沙箱环境充分测试、准备详细日志排查 |
| NISP 身份证→PDF 转换质量 | 中 | NISP 报名无法使用 | 评估 reportlab/img2pdf 库、准备降级为图片上传 |
| 题库 Excel 格式不标准 | 高 | 批量导入失败 | 提供标准化 Excel 模板、导入工具带格式校验 + 错误行提示 |
| OSS 视频存储成本超预期 | 低 | 运营成本增加 | 设置 CDN 缓存策略、冷数据归档、视频转码压缩 |
| 并发支付导致订单重复 | 中 | 资金风险 | Redis 分布式锁 + 数据库唯一索引 + 幂等处理 |

---

> **编制依据**：架构图.md（ER 图 + 模块汇总）+ 全期开发计划 + 项目全模块清单
> **编制日期**：2026-05-14
